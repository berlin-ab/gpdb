platform: linux
inputs:
  - name: gpdb_src
run:
  path: ./concourse/pr_pipeline/runner.bash
  dir: gpdb_src
params:
  # default to empty, override from pipeline
  CONFIGURE_FLAGS:
caches:
  - path: gpdb_src/.ccache
  
